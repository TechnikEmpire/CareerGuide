# Initial Python environment for the first implementation slice.
# We are keeping this small on purpose so the local environment stays easy to create.
# Heavier model and ingestion dependencies can be added once the core scaffold is stable.
# One-time ESCO translation tooling has its own WSL2/GPU-oriented requirements under
# `tooling/translation/requirements.txt`.
# Real local GGUF generation runtime dependencies live in `requirements-runtime.txt`
# so the main app environment does not force a native inference build by default.
faiss-cpu
fastapi
huggingface-hub>=1.3,<2
httpx
numpy
pydantic
pydantic-settings
pytest
sentence-transformers>=2.7.0
sqlalchemy
tqdm
transformers>=4.51.0
uvicorn[standard]
